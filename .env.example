# ENVIRONMENT VARIABLES - REQUIRED FOR BACKEND EXECUTION
# COPY THIS FILE TO .env AND FILL IN THE REAL VALUES

# Database Credentials
# MySQL connection details. Ensure the database (intern_assignment_db) exists.
DB_USER=your_mysql_username  # e.g., root, user_dev
DB_PASSWORD=your_mysql_password # MUST be set for security
DB_HOST=localhost

# Database Name (MUST match the name created in your MySQL server)
DB_NAME=intern_assignment_db

# Security Settings
# CRITICAL: This MUST be a long, random, alphanumeric string (e.g., 32+ characters)
SECRET_KEY=paste_a_long_random_jwt_secret_here

# JWT Algorithm (DO NOT CHANGE)
ALGORITHM=HS256

# Token Expiration Time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30
